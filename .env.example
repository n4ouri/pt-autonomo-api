PORT=3000
DATABASE_PATH=./data/autonomo.db

# Required only if you use the optional connector layer (POST/PUT /api/v1/me/connectors/*)
# to store an encrypted session cookie for Finanças/Segurança Social sync. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CONNECTOR_ENCRYPTION_KEY=
